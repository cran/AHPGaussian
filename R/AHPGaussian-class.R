AHPGaussian <- setRefClass("AHPGaussian",
                         methods = list(
                           ahpgaussian = function(x) {
                             results <- ahpgaussian(x)
                             return(results)
                           }
                         )
)
